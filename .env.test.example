# Test Environment Configuration
# Copy this file to .env.test and fill in your test Supabase project credentials

# Supabase Test Project URL
VITE_SUPABASE_URL=https://your-test-project.supabase.co

# Supabase Anon Key (public key for client-side operations)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (admin key for teardown operations)
# ⚠️ NEVER commit this key to version control
# Get from: Supabase Dashboard > Settings > API > service_role key (secret)
VITE_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Test User Credentials (permanent user for authenticated session reuse)
# This user should be manually created in your test Supabase project
E2E_USERNAME_ID=uuid-of-test-user
E2E_USERNAME=test-user@example.com
E2E_PASSWORD=test-password-here
